server:
  port: 8888

spring:
  application:
    name: lost-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shibo92/lost.git
          search-paths: lost-config-repo/conf
          force-pull: true
eureka:
  client:
    service-url:
      defaultZone: http://lost-eureka-server-host:8761/eureka/
